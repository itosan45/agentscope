services:
  # バックエンド (API)
  - type: web
    name: agentscope-backend
    runtime: docker
    plan: free
    envVars:
      - key: DATABASE_URL
        value: sqlite:///./agentscope.db
    rootDir: backend

  # 管理ダッシュボード
  - type: web
    name: agentscope-dashboard
    runtime: docker
    plan: free
    rootDir: frontend

  # ランディングページ (世界への顔)
  - type: web
    name: agentscope-landing
    runtime: docker
    plan: free
    rootDir: landing
